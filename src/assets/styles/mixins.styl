vw($num, $def = 1280) {
    return ($num * 100 / $def) vw;
}

// https://benmarshall.me/convert-photoshop-letter-spacing-to-css/
letterSpacingPH($letter-spacing-PH, $font-size = 24) {
    return ($letter-spacing-PH * $font-size / 1000) px;
}